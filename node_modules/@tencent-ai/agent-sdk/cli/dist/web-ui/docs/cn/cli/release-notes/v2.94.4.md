# 🚀 CodeBuddy Code v2.94.4 发布

## 🐛 问题修复

- **自定义模型兼容性**：修复自定义模型（如 `custom-local:xxx`）在通过第三方 OpenAI 兼容接口调用时，请求体中残留的不兼容字段（`verbosity`、`reasoning_summary`、`reasoning_effort`）导致接口返回 400 错误的问题，确保自定义模型能够正常工作
