# 🚀 CodeBuddy Code v2.93.2 发布

## 🐛 问题修复

- **文件树加载恢复**：修复持久化数据恢复异常导致文件树永远显示"加载失败"的问题，新增重试按钮支持手动恢复
- **持久化数据完整性**：修复 Set/Map 类型字段持久化后恢复为空对象的问题，保存前自动转换为可序列化格式
- **Marketplace 存储稳定性**：为市场配置文件引入跨进程文件锁与原子写入，修复多进程并发写入时的数据损坏问题；读取到损坏文件时自动备份并重建
- **对话历史回放**：修复斜杠命令（如 `/clear`）在历史回放时被错误截断的问题，确保命令内容完整还原
