# 🚀 CodeBuddy Code v2.33.1 发布

## 🔧 改进优化

- **会话上下文类型优化**：重构会话、检查点和上下文管理的类型定义，增强输入项处理和历史记录工具的类型安全性
- **命令权限检查**：优化白名单命令链的权限判断，当所有子命令都在安全白名单中时直接放行，无需额外确认
- **Memory 加载器**：修复暂时禁用的子目录 Memory 加载方法导致的编译警告
