# 🚀 CodeBuddy Code v2.37.20 发布

## 🔧 改进优化

- **模型临时切换**：在模型选择面板按 `t` 键可仅对当前会话生效，不修改用户配置，方便临时测试不同模型
- **--model 参数优化**：命令行 `--model` 参数现在正确覆盖项目级配置，当前会话立即生效
- **上下文压缩优化**：压缩时过滤规则和记忆内容，避免将配置指令压缩进摘要，压缩后自动重新注入完整规则，保持 AI 行为一致性
- **配置面板精简**：隐藏开发者选项，需通过 `/config --dev` 访问，让配置界面更简洁
