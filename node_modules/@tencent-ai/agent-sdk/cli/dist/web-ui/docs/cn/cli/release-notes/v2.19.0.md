# 🚀 CodeBuddy Code v2.19.0 发布

## ✨ 新功能

- 会话重命名：新增 `/rename` 命令，支持快速重命名当前会话，可直接传参或通过交互式面板操作
- 输入撤销：新增 `Ctrl+Z` 快捷键支持，可随时撤销输入框中的文本编辑

## 🔧 改进优化

- 会话恢复面板：优化会话恢复界面，提供更直观的会话管理体验
- 连接诊断增强：改进网络连接断开场景的诊断信息，便于问题排查
