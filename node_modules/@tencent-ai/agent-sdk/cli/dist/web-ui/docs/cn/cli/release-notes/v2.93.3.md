# 🚀 CodeBuddy Code v2.93.3 发布

## 🔧 改进优化

- **权限系统优化**：已信任目录的项目级 allow 规则现在可以覆盖高危命令拦截，无需手动将规则移到用户级配置。未信任目录的行为保持不变
- **CORS 通配符支持**：CORS 白名单新增子域通配（`https://*.example.com`）和全部允许（`*`）两种配置模式，方便多子域环境快速接入
- **局域网访问简化**：使用 `--host 0.0.0.0` 启动时，自动将本机局域网 IP 加入 CORS 和 Host 白名单，局域网设备可直接通过 IP 访问 Web UI，无需额外配置
