# 🚀 CodeBuddy Code v2.66.2 Release

## 🐛 Bug Fixes

- **Model settings sync**: Fixed an issue where options were not synchronized when setting the model via SDK/control signals, ensuring model selection remains consistent across all read paths
