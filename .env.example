# ============================================================
# ScriptCraft AI - 环境变量配置示例
# 复制此文件为 .env 并填写真实值
# ============================================================

# ------ 后端（Railway 部署时使用） ------
# MongoDB Atlas 连接字符串（必填）
# 格式：mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>
MONGODB_URI=

# 服务器端口（Railway 会自动设置 PORT 环境变量）
# PORT=3001

# ------ 前端（Vercel 部署时使用） ------
# 后端 API 地址（必填）
# 例如：https://scriptcraft-ai-production.railway.app
VITE_API_BASE_URL=

# ------ 说明 ------
# 本地开发时：
#   - 不需要设置 MONGODB_URI（会自动使用 JSON 文件存储）
#   - VITE_API_BASE_URL 不需要设置（默认使用 /api，由 Vite 代理到本地后端）
#
# Railway 部署时：
#   - 在 Railway 控制台设置 MONGODB_URI 环境变量
#   - 不需要设置 VITE_API_BASE_URL（这是前端环境变量）
#
# Vercel 部署时：
#   - 在 Vercel 控制台设置 VITE_API_BASE_URL 环境变量
#   - 值填你的 Railway 后端地址，例如：https://scriptcraft-ai-production.railway.app
